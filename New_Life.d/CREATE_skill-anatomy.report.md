# CREATE · `skill-anatomy`

## 1 · Три проверки

**1. Что уже стоит на этой земле — исход: та же земля не занята; коллизий (спорящих правил) нет; есть три адреса той же логики и одно стоячее нарушение.** Обыскивал тела всего, что в силе, не строки границ.

*Блоки 90 и 91 против раздела 1 `/home/user/Skill-Factory/skills/agent-readable/SKILL.md`* (проверено — прочитал файл целиком):

- Блок 90, материал: «Объясняй почему, а не только что. Правило, чьё рассуждение записано, — это правило, которое можно применить к ситуации, которую никто не предвидел. Голый императив можно только исполнить или проигнорировать.»
  `agent-readable`, раздел 1: «**Правило без рассуждения — не сжатие, а порча.** Голый императив исполняется или игнорируется; правило с записанным рассуждением применяется к случаю, который никто не предвидел.»
- Блок 91, материал: «Длина — это цена. Каждая строка, добавленная к скиллу, разбавляет остальное и приближает хвост файла к тому, что сжатие длинной сессии срезает молча (§3, второе свойство среды). Добавить правило — значит уметь сказать, чего оно стоит.»
  `agent-readable`, раздел 1: «**Минимум токенов сам по себе — не критерий.** Критерий — минимум при сохранённом списке указаний и сохранённой однозначности. Без названного сохраняемого свойства правка вырождается в «короче значит лучше».»

Это не коллизия: тексты не спорят, они — одна логика с двух сторон. Блоки 90–91 судят *добавление* (чего стоит строка, несёт ли она своё «почему»); `agent-readable` судит *рез* (нельзя срезать «почему», «короче» — не критерий). Вопросы разные, и `agent-readable` сам выносит за скобки «стоит ли принимать эту правку по существу» — ровно то, что берёт `skill-anatomy`. Третий адрес той же логики блока 90 — `/home/user/Skill-Factory/skills/git-repo-structure/SKILL.md`, раздел 2, о решениях в памяти проекта: «Каждое решение несёт краткое «почему» — решение без рассуждения можно только исполнить или проигнорировать, но не применить к случаю, которого никто не предвидел.»

Как быть — предлагаю, не сливая: **оставить оба, каждый в форме своего вопроса** (вариант A). Каждая формулировка держит собственный тест своего файла, и каждая папка обязана работать в одиночку — указатель вместо правила стал бы зависимостью. Цена — риск разъезда двух редакций; его ловит правило REVIEW «у правила бывает несколько адресов». Отвергнутые варианты — в разделе 4, вопрос 1.

*Прочие пересечения, все согласные:*

- Блок 56 (граница по последствию) стоит вторым адресом в `git-repo-structure`, раздел 2: «Тест принадлежности: запись меняет то, *как об этом говорят*. Если она меняет то, *что́ считается сделанным правильно*, — это метод, и он входит в скилл.» Тот же тест с другой стороны (что идёт в память проекта). Не коллизия; при переписывании одного надо трогать второй.
- Блок 50 (верх переживает сжатие, глубина — в references) соседствует с заявкой `agent-readable`: «Сюда же неотделимая половина: где в файле стоит правило.» Разная крупность: у соседа — раздел внутри файла, у анатомии — `SKILL.md` против справочных файлов и верх против хвоста. Согласны.
- Новая редакция блока 40 совпадает с тем, что уже стоит в `/home/user/Skill-Factory/agents/skill-master/AGENT.md`: «границы каждого скилла называют вопрос, на который он не отвечает, а не имя соседа» (CREATE, проверка 1) и «скилл, чьи границы названы именами соседей, а не вопросами: он перестаёт работать там, где соседа нет» (REVIEW, проход дрейфа). Рассуждение из второй строки взял в артефакт.
- Блоки 87, 89, 91 уже пересказаны внутри `AGENT.md`: шаг 1 REVIEW («правило, чинящее ровно наблюдавшийся случай и ничего соседнего, — переобучение»), проход «Компактность» («лишняя строка разбавляет соседние и съедает бюджет сжатия»), проход «Мёртвое» («два слабых скилла, ставших одним сильным…»). После ввода скилла это метод в теле агента — кандидат на укорачивание, не поломка; см. проводку.
- Блок 68 («писать скилл на вырост нормально») против `JUDGEMENT.md` («Правило, добавленное потому, что звучало правильно, без сигнала за ним») — не спорят: первое о том, что грубый файл вправе существовать по решению владельца, второе о том, что предложение без сигнала помечается как мнение.

*Стоячее нарушение, не созданное артефактом, но попадающее в его предмет:* четыре скилла в силе — `confidence-check`, `verify-before-done`, `signal-capture`, `git-repo-structure` — несут раздел `## Предпочтения владельца` (проверено — прочитал все шесть тел). Пример строки, обоснованной авторством и историей, `/home/user/Skill-Factory/skills/confidence-check/SKILL.md`: «**Пометки уверенности ожидаются, а не факультативны.** Они вошли в рабочий контракт после исследования источников, в котором песочница молча заблокировала несколько проверок». Блоки 53–54 это запрещают, и запрет уже в силе — в философии («Метод и манера») и последней строкой `JUDGEMENT.md`. Скилл не создаёт конфликт, он делает его своим делом. Чинится разбором, не CREATE — вопрос 2.

**2. Скилл или агент — исход: скилл.** Ни один из пяти тестов не держится: метод читает тот, кто пишет артефакт, в своём же контексте; параллелизма нет; состязательная независимость нужна проверяющему, а не пишущему, и её уже покупает агент библиотеки; прав и модели других не надо; триггер — сама текущая задача. Задание агента и не просило.

**3. Различие — исход: формулируется.** Без него артефакт пишут по образцу соседа: агента заводят там, где хватило бы скилла, правило кладут под ближайший заголовок, границы называют соседей, description описывает вместо того, чтобы срабатывать. С ним кандидат проходит четверо ворот до первой строки, форму выбирают пять тестов, различие и границы стоят там, где их не срежет сжатие, а правка судится пятью эвристиками. Полный текст — первым абзацем артефакта.

Ворота на самом артефакте: один принцип — форма и допуск артефакта; достижим — description несёт «скилл или агент», «frontmatter», «границы», «новый скилл», «правка к скиллу», каталог собирает первую фразу сам; форму менять умеет.

## 2 · Артефакт

**Путь:** `/home/user/Skill-Factory/skills/skill-anatomy/SKILL.md` — сразу в силе, по заданию. Папка состоит из одного файла: справочные файлы не нужны (деревья и таблица коротки и нужны в момент письма; файл из одной таблицы — погоня за указателем), скриптов и шаблонов у метода нет.

````markdown
---
name: skill-anatomy
description: Определяет, как пишется скилл или агент этой библиотеки — четверо ворот перед первой строкой, пять тестов «скилл или агент», анатомия папки, frontmatter и тела, эвристики суждения о правке. Применяй, когда решено написать новый скилл, агента или команду, и прежде чем предлагать правку к существующему скиллу. Применяй, когда спорят, скилл это или агент, когда скилл кажется делающим слишком много, когда правило подвернулось по ходу письма и просится в файл, и когда description не срабатывает.
---

# Skill anatomy

**Что меняется из-за этого скилла:** без него артефакт пишут по образцу
ближайшего соседа: агента заводят там, где хватило бы скилла, правило кладут под
ближайший заголовок, границы называют соседей, description описывает вместо
того, чтобы срабатывать, и никто не может сказать одним предложением, зачем файл
существует. С ним кандидат проходит четверо ворот до первой строки, форму
выбирают пять тестов, каждый файл несёт фразу различия и границы там, где их не
срежет сжатие, а правка судится пятью эвристиками, а не тем, что звучит
правильно.

**Покрывает:** форму и допуск артефакта — вправе ли он существовать, скилл это,
агент или команда, из чего состоят его папка, frontmatter и тело, и чем судят
правку к уже написанному.
**Оставляет за скобками:** «прочтёт ли исполнитель этот текст однозначно и не
платит ли за него лишним контекстом» — вопрос о редакции написанного, не о его
форме и допуске · «верно ли правило по существу в своей области» — здесь судят
место и цену правила, не его истину · «что случилось в работе и стоит ли это
записать» — сырьё для правки, а не правило · «как ведётся разбор и кто решает» —
здесь только то, чем судят · «как автор хочет, чтобы с ним разговаривали» —
манера живёт в рабочем соглашении проекта · «как устроен репозиторий вокруг
артефакта» — корень, имена, ветки, коммиты.
**Не грузи меня** ради правки, которая не добавляет и не снимает правила и не
меняет форму файла: перестановку слов судят по другому вопросу — читается ли
текст так, как задумано.

---

## 1. Ворота: когда скиллу позволено существовать

Четверо ворот. Кандидат, проваливший любые, не пишется.

**Тест на различие.** Скажи одним предложением, что́ меняется в поведении со
скиллом и без него. Если это предложение написать нельзя — скилл декоративен. Эта
фраза обязательна в каждом `SKILL.md` и проверяется первой при любой правке.

**Один скилл — одна задача или один принцип**, даже когда принцип велик и
фундаментален. Тест не в размере, а в связности. Длинный скилл про одно —
здоровый. Короткий, несущий два несвязанных правила, — нет, каким бы аккуратным
он ни выглядел.

Если описанию нужно «и», чтобы соединить две работы, не зависящие друг от друга,
это два скилла. Пересечение соседей нормально; объединение двух методов — нет.

Эти ворота чаще всего проваливают по случайности, потому что правило *истинное*,
*полезное* и *подвернувшееся по ходу письма* кажется уместным. Оно неуместно:
скиллы грузятся выборочно, и правило, подшитое под чужой заголовок, — это
правило, которое будет отсутствовать, когда действительно нужно, и присутствовать,
когда является шумом. Два хороших правила, не связанных между собой, — два
скилла, а не один.

**До него должно быть достижимо.** Скилл, к которому никто и ничто никогда не
проложит маршрут, мёртв при рождении. Либо описание несёт слова, которые владелец
употребил бы естественно, либо каталог ставит его перед нужной задачей.

**Он должен уметь менять форму.** Скилл, переставший окупаться, не удаляется — он
сливается, переписывается или, только с явного разрешения владельца, отправляется
на покой. Поэтому писать скилл на вырост нормально: грубый скилл, который может
пригодиться, куда дешевле переформовать потом, чем изобрести с нуля, а
существующий файл — это файл, на который владелец может отреагировать.

## 2. Как судить правку

Пять эвристик. Они для каждого, кто предлагает или принимает правку к скиллу, а
не только для разбора.

**Предпочитай изменение, которое обобщается.** Сигналы — горстка примеров; скилл
отработает на случаях, которых сигналы не видели. Правило, чинящее ровно
наблюдавшийся случай и ничего соседнего, — переобучение, и оно делает файл
длиннее, не делая лучше.

**Предпочитай переписать правило, а не оговорить его.** Когда правило внутри
скилла даёт осечку, рефлекс — добавить исключение. Два исключения на одно правило
значат, что правило было неверным: переформулируй его так, чтобы исключения стали
не нужны. Это про строки внутри файла; целый скилл, переставший окупаться, идёт
по четвёртым воротам — слияние первым, и решает владелец.

**Предпочитай слить, а не добавить.** Прежде чем писать новый скилл, проверь, не
является ли существующий той же работой, увиденной под другим углом. Один сильный
скилл лучше двух, каждый из которых покрывает половину, а проверка стоит
несравнимо меньше второго файла.

**Объясняй почему, а не только что.** Правило, чьё рассуждение записано, — это
правило, которое можно применить к ситуации, которую никто не предвидел. Голый
императив можно только исполнить или проигнорировать.

**Длина — это цена.** Каждая строка, добавленная к скиллу, разбавляет остальное и
приближает хвост файла к тому, что сжатие длинной сессии срезает молча. Добавить
правило — значит уметь сказать, чего оно стоит.

## 3. Скилл, агент или команда

Скилл меняет, *как* работа делается: его читает тот, кто уже работает, и у него
нет своего контекста, инструментов или модели. Агент меняет, *кто* её делает: он
живёт в своём окне контекста, со своим списком инструментов и, возможно, своей
моделью, и ему делегируют.

По умолчанию пиши скилл. Повышай до агента, только если верно хотя бы одно:

1. **Изоляция контекста и есть суть.** Работа порождает намного больше шума, чем
   вывода, — перечитать половину репозитория, чтобы ответить на один вопрос.
   Агент поглощает шум в другом месте.
2. **Нужен параллелизм.** Несколько экземпляров одновременно, на независимых
   кусках.
3. **Нужна состязательная независимость.** Проверяющий не должен был видеть, как
   вещь строилась. Рецензент, прочитавший рассуждение, уже им заражён; это
   единственный способ купить по-настоящему свежий взгляд.
4. **Другой профиль прав или другая модель.** Только чтение, модель подешевле,
   более узкий набор инструментов.
5. **Отдельный триггер.** Он срабатывает на событие или границу, а не на текущую
   задачу.

Ни одного верного — значит это скилл. Шестой причины не существует.

**Изоляция доступна без новой личности.** Метод, которому нужен свой контекст,
можно запустить в отдельном контексте, оставаясь скиллом. Тянись за этим прежде,
чем изобретать агента: пять тестов — про личность, а не только про изоляцию.

**Файл агента — список чтения, а не метод.** Агент, чей файл несёт метод вместо
ссылок на скиллы, — это скилл в костюме. Хороший файл агента короток: кто он,
когда включается, какие скиллы читает, что обязано быть в его выводе, чего он не
должен касаться. Метод остаётся в скиллах, где до него может дотянуться всё
остальное.

**Команда — третья форма, а не третья сущность.** Команда — это скилл с
`disable-model-invocation: true`: вызывается владельцем, никогда самой моделью.
Применяй её для всего, что имеет побочные эффекты, или где момент выбирает
владелец.

Команда покупает триггер, но никогда способность. Всё, что она делала бы,
достижимо просьбой, поэтому форма оправдывает свой файл только там, где важен сам
триггер: где модель не должна запускать это сама, или где ничто в сессии не
разделяет контекст, нужный для внятной просьбы. Где это не так, команда — второе
имя для того, что уже есть.

## 4. Папка

### Скилл

```
<skill-name>/
├── SKILL.md          обязателен — frontmatter + метод
├── references/       грузится по требованию, не заранее
├── scripts/          исполняемое, для всего детерминированного и повторяемого
└── assets/           шаблоны, файлы, попадающие в результат
```

Тело `SKILL.md` открывается фразой различия, за ней — две строки **границ**: что
скилл покрывает и на какой вопрос намеренно не отвечает. Они стоят наверху,
потому что тот, кто составляет список чтения, судит по ним, не открывая тела, и
потому что начало — единственное, что гарантированно переживает длинную сессию.

Границы называют вопрос, а не имя соседа: имя работает только там, где сосед под
рукой, а папку отправляют и в чат, где нет больше ничего; вопрос остаётся верным
везде. Записанные границы — это то, что не даёт двум скиллам навязывать одной
задаче противоречащие правила, и это первое, что надо проверить, когда скилл
начинает казаться делающим слишком много.

**Файл держит метод и ничего о себе:** ни журнала изменений, ни статуса, ни
версии — что с ним происходило, знает git. Единственное исключение — черновик,
ещё не введённый в силу, который может заканчиваться разделом
**`## Open questions for the owner`**: нерешённые вопросы о границах, которые его
автор не имел права закрывать. Этот раздел — повестка того разговора, который
решает судьбу черновика, и он снимается при вводе в силу: к тому моменту у
вопросов есть ответы.

### Агент

```
<agent-name>/
├── AGENT.md          валидный файл агента: скопируй — и он работает
├── README.md         как поставить его в проект или в другой чат
├── bundled/          штампованные копии того, что ему нужно
└── <его состояние>   знание, которое агент накапливает
```

Папка `bundled` существует потому, что правило «папка работает в одиночку,
отправленная в чат, где нет больше ничего» старше, чем избегание дублирования.
Копия производна: она несёт штамп с именем источника и датой и никогда не
правится на месте — правится канон, а копия перештамповывается.

## 5. Frontmatter

Держи только то, что используется. `name` и `description` всегда; остальное —
только когда оно что-то делает.

| Поле | Назначение |
| --- | --- |
| `name` | равно имени папки |
| `description` | что делает **и когда применять** |
| `when_to_use` | дополнительные триггерные фразы |
| `disable-model-invocation: true` | делает его командой |
| `user-invocable: false` | фоновое знание, а не действие |
| `allowed-tools` | предодобренные инструменты на вызывающий ход |

**`description` — это триггер, а не ярлык.** Модель видит только имя и описание,
прежде чем решить, открывать ли скилл: тело до этого невидимо. Пиши его как *что
он делает плюс когда за ним тянуться*, ключевой случай первым.

**Описания конкурируют за общий бюджет.** Описание каждого скилла лежит в
контексте, чтобы модель знала о его существовании, и этот перечень ограничен. По
мере роста библиотеки описания обрезаются — а обрезается хвост, где обычно и
стоят триггерные слова. Ключевой случай впереди — это ещё и то, что переживёт
обрезку.

## 6. Тело

Два свойства среды, из которых следует форма тела. Они держатся поперёк моделей и
платформ, потому что следуют из того, как устроено окно контекста, а не из
настроек одного продукта. Точные цифры за ними движутся; форма — нет. Цифры в
правила не пишутся вовсе: изменившийся лимит никогда не должен отменять принцип.

**Скилл читается один раз и остаётся; на последующих ходах он не перечитывается.**
Пиши стоячие правила, действующие до конца сессии, а не процедуру, которую надо
пройти. «С этого момента помечай каждый факт его уверенностью» переживает всю
сессию; «шаг 1, шаг 2, шаг 3» израсходован в момент прочтения.

**Только начало скилла гарантированно переживает длинную сессию.** Когда контекст
заполняется, он сжимается, и то, что переносится дальше, ограничено. Клади
правила, которые нельзя терять, ближе к верху, а глубину выталкивай в справочные
файлы — папку `references` из дерева выше. Длинный скилл теряет свой хвост
молча — ровно в тех длинных сессиях, где он важнее всего.

### Метод, не манера

**Скилл целиком состоит из метода.** Отдельного слоя предпочтений в нём нет:
идея, поданная владельцем, — всё равно метод, она принадлежит телу и держится
своим рассуждением, а не тем, кто её произнёс. Такой скилл можно отдать кому
угодно, не переписывая.

Отсюда запрет: **«это его явное указание» — не довод внутри скилла.** Правило,
обоснованное авторством, нельзя ни проверить, ни оспорить, и застывает оно первым.
Пишется рассуждение; чьё оно, знает git.

**Граница проходит по последствию, а не по авторству.** Предпочтение, меняющее,
*что́ считается сделанным правильно*, — метод, и оно входит в скилл, которому
принадлежит. Предпочтение, меняющее, *как об этом говорят*, — манера, и в скилле
ей места нет.
````

**Что изменено относительно материала и почему** — каждое отклонение названо, чтобы владелец проверял текст, а не искал разницу сам:

- **Блок 40 — новая редакция по решению 2026-09-09.** Было: «что намеренно оставляет названному соседу». Стало: «на какой вопрос намеренно не отвечает», плюс рассуждение: имя соседа работает только там, где сосед под рукой, а папку отправляют и в пустой чат; вопрос верен везде. Довод взят из строки прохода дрейфа в `AGENT.md`.
- **Блок 33 «прочитать сорок файлов» → «перечитать половину репозитория»; блок 36 «триста строк метода» → «метод вместо ссылок на скиллы»; блок 87 «отработает сотни раз» → «отработает на случаях, которых сигналы не видели»; блок 89 «проверка стоит минуты» → «стоит несравнимо меньше второго файла».** Числа для веса, не для счёта, — по условию задания; «два исключения» (блок 88) оставлено: это счёт-порог, и он и есть эвристика.
- **Блок 91 «(§3, второе свойство среды)»** — ссылка снята: фраза «сжатие длинной сессии срезает молча» держится сама, свойство раскрыто в разделе 6.
- **Блок 88 «по §5, где слияние идёт первым»** → «по четвёртым воротам — слияние первым»: внутренняя ссылка на раздел 1 этого же файла.
- **Блок 52** (изоляция без личности) перенесён из «свойств среды» вплотную к пяти тестам: он охраняет именно решение «скилл или агент», и потерять его в хвосте значит завести лишнего агента. Отсюда в разделе 6 «два свойства», а не четыре; **блок 51** (бюджет описаний) стоит рядом с блоком 44, чтобы «ключевой случай первым» не звучало дважды в далёких разделах.
- **Блок 42**, вторая фраза («отношение копии к оригиналу … в разделе «Как части соединены»») заменена самим правилом формы: копия штампована именем источника и датой, не правится на месте. Скилл обязан работать без философии; конкретные связи (что именно у какого агента в `bundled/`) остаются в карте.
- **Блок 86** не перенесён — анонс, как и оговорено.
- **Добавлено сверх 35 блоков, по одной строке каждое, ради самодостаточности:** определения скилла и агента в раздел 3 (тесты непонятны без них; определения остаются и в философии); базовое правило «файл держит метод и ничего о себе» перед исключением блока 47 (исключение без правила не читается); место фразы различия и границ — первым абзацем и сразу за ним — с рассуждением; строка «Не грузи меня» для того, кто составляет список чтения; одна вводная фраза перед эвристиками вместо блока 86.
- **Порядок разделов — не порядок философии.** Ворота и эвристики стоят наверху, деревья и таблица — внизу: по собственному правилу скилла верх переживает сжатие, и терять нельзя именно то, что проваливают чаще всего (ворота связности, эвристики), а дерево папки проверяется взглядом на соседа. Если владелец хочет порядок философии, перестановка стоит одной правки и не трогает текст.
- **Имена папок в прозе без косой черты** (`references`, `bundled`): `/home/user/Skill-Factory/.claude/scripts/check_links.py` (проверено — прочитал) считает строку в обратных кавычках, начинающуюся с `references/`, `bundled/`, `scripts/`, `assets/`, указателем внутри артефакта и искал бы эти папки в `skills/skill-anatomy/`, которых нет. Деревья в блоках кода скрипт не трогает.

## 3 · Проводка

- **Сломалось: `/home/user/Skill-Factory/agents/skill-master/AGENT.md`, работа CREATE.** «Читаешь обязательно: `bundled/PROJECT_PHILOSOPHY.md` — анатомия, ворота, пять тестов» перестаёт быть правдой после выселения абзацев; «Затем пиши текст по анатомии из философии» — тоже. Список чтения должен назвать этот скилл (по заданию — делает сессия); куда именно указывать — вопрос 3.
- **Сломалось: там же, работа MINE, шаг 3:** «примени ворота из философии вслух» — ворот в философии не будет. MINE читает «всё, что в силе», так что скилл он прочтёт; неверным становится слово «из философии».
- **Сломалось: `/home/user/Skill-Factory/agents/skill-master/bundled/MANIFEST.md` и папка `bundled/`.** «Философия держит каждое правило, которому обязан подчиняться артефакт: анатомию, ворота, петлю разбора» — после переезда анатомии и ворот там нет, а папка агента обязана работать в одиночку и CREATE читает именно этот скилл: нужна штампованная копия `skill-anatomy` и строка в манифесте; «Почему именно эти два» становится «три». Та же строка в `/home/user/Skill-Factory/agents/skill-master/README.md`: «`bundled/` | штампованные копии философии и `confidence-check`».
- **Сломается, если не перештамповать:** после правки `PROJECT_PHILOSOPHY_CHECK.md` копия `bundled/PROJECT_PHILOSOPHY.md` (штамп 2026-09-10) разойдётся с каноном — прогон синхронизации обязателен.
- **Философия:** карта «Как части соединены» получает строку (по заданию, сессия); раздел философии, озаглавленный «Анатомия», после выселения держит только правило об истории и правило о манере — заголовок перестаёт отвечать содержимому, это замечание сессии, не моё решение.
- **Не сломалось, но требует прогона:** каталог в `/home/user/Skill-Factory/README.md` собирается скриптом из первой фразы description; до перезапуска `build_catalogue.py` проверка `--check` красная (проверено — прочитал скрипт).
- **Не сломалось:** заглушка `/home/user/Skill-Factory/.claude/agents/skill-master.md` — правило приставки покрывает и `bundled/skill-anatomy.md`; ROUTE читает frontmatter и строки границ, скилл в них укладывается; REVIEW читает всё в силе; `JUDGEMENT.md` не меняется.
- **Перестало окупаться:** пересказы эвристик внутри `AGENT.md` (шаг 1 REVIEW, проходы «Компактность» и «Мёртвое», проверка 1 CREATE) — с вводом скилла это метод в теле агента; укорачивание до указателей — предложение для разбора, не для этого шага.

## 4 · Вопросы

1. **Одна логика блоков 90–91 на двух адресах** — `skill-anatomy` (сторона добавления) и раздел 1 `agent-readable` (сторона реза), а для блока 90 ещё и в `git-repo-structure` о решениях памяти. Вопрос возник сейчас, потому что до ввода скилла второго адреса в силе не было. Варианты: **A — оставить оба как есть** (рекомендую: каждый держит собственный тест, папки работают в одиночку, вопросы разные); B — ужать две формулировки `agent-readable` до одной строки каждая с указателем на `skill-anatomy` (указатель становится зависимостью, тест реза теряет опору); C — убрать 90–91 из `skill-anatomy` и отсылать к соседу (сосед сам выносит «стоит ли принимать правку по существу» за скобки). Ответ различит, останется ли у `agent-readable` собственная защита «почему» от реза.
2. **Разделы `## Предпочтения владельца` в четырёх скиллах в силе** — стоячее нарушение блоков 53–54, которые с этим скиллом получают свой файл. Ставлю сейчас, потому что ввод скилла делает нарушение его предметом, а чинить его в CREATE я не вправе. Варианты: **A — отдельный REVIEW с фокусом «разделы предпочтений»** (рекомендую: часть пунктов — метод в костюме манеры, часть — история и манера, решать надо по строке); B — оставить до планового разбора. Ответ различит, когда сессия это поднимет.
3. **Куда указывает список чтения CREATE** — `skills/skill-anatomy/SKILL.md` от корня или `bundled/skill-anatomy.md` рядом с агентом. Варианты: **A — штампованная копия в `bundled/`** (рекомендую: тот же узор, что у философии, папка агента работает в одиночку, заглушка добавляет приставку); B — путь от корня (короче, но папка агента, отправленная в чужой чат, теряет метод письма).
4. **Таблица полей frontmatter** (`when_to_use`, `user-invocable`, `allowed-tools`) перенесена из философии как есть: видел — в документе владельца; против документации платформы не проверял, инструмента для этого у меня нет. Правило `confidence-check` о поведении платформы говорит проверять до того, как на этом строят, а по этой таблице будут писать все будущие скиллы. Варианты: **A — сессия сверяет поля с документацией один раз до ввода** (рекомендую); B — вводить как есть, таблица уже в силе в философии.

## 5 · Источники

Целиком (проверено — прочитал): `/home/user/Skill-Factory/.claude/agents/skill-master.md`; `/home/user/Skill-Factory/agents/skill-master/AGENT.md`, `README.md`, `JUDGEMENT.md`, `bundled/MANIFEST.md`, `bundled/PROJECT_PHILOSOPHY.md` (штамп 2026-09-10), `bundled/confidence-check.md`; все шесть `SKILL.md` в `/home/user/Skill-Factory/skills/` (`agent-readable`, `confidence-check`, `git-repo-structure`, `signal-capture`, `stress-run`, `verify-before-done`), `skills/signal-capture/references/INSTALL.md`, `skills/signal-capture/scripts/signals_check.sh`, `skills/git-repo-structure/references/FINAL_PASS.md`; `/home/user/Skill-Factory/README.md`; `/home/user/Skill-Factory/.claude/scripts/build_catalogue.py`, `check_links.py`.

Частично: `/home/user/Skill-Factory/PROJECT_PHILOSOPHY_CHECK.md` — только grep по четырём фразам блоков 33, 36, 40, 86: канон несёт их в том же виде, что и копия (проверено); целиком опирался на копию.

Не читал: `PROJECT_IDEAS_CHECK.md`, `New_Life.md`, `New_Life.d/` — по запрету задания (`New_Life.md` всплыл только путём в поиске «skill-anatomy», не открывал); `PROJECT_MEMORY_CHECK.md` — не в списке чтения CREATE; `to_review/`, `archive/` — не в силе; `.claude/scripts/sync_bundles.py`, `check_signals.sh`, `.claude/settings.json`; наличие `SIGNALS.md` в корне не проверял.
