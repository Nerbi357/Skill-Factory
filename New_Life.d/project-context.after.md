# `project-context` — список указаний после правки

Восстановлен 2026-09-13 по редакции `skills/project-context/SKILL.md` (124 строки).

**Резов нет, и это результат, а не пропуск работы.** Файл написан прогоном CREATE
в тот же день по анатомии библиотеки и по линии реза, названной арбитром развилки
5.3; признаки скилла `agent-readable` проверены по нему все двенадцать, и ни один
не сработал: повторов внутри абзацев и между соседними нет, определений
очевидного нет, вторых примеров нет, концовок-итогов нет, чисел для веса нет,
правил не тому читателю нет. Оба списка совпадают построчно: тридцать четыре
указания, тот же порядок, лишних нет.

Вместо резов петля сделала здесь другую работу — сверила, что уехало ровно то, что
должно было уехать.

## Сверка с линией реза арбитра

Арбитр назвал шесть вещей, уходящих в новый скилл. Все шесть на месте:

| Что должно было уехать | Где оно в новом скилле |
| --- | --- |
| что лежит внутри каждого из трёх документов | разделы 2, 3, 4 |
| тест свежей сессии | раздел 2, цитатой |
| два обязательных блока философии и её отдельная читаемость | раздел 4 |
| дом рабочего соглашения с тестом принадлежности | раздел 2, подраздел «Рабочее соглашение» |
| метод фаз | раздел 2, подраздел «План ведётся фазами» |
| довод «слить их означало бы получить документ, который переписывается целиком» | раздел 1 |

И ничего сверх: указания 7–11 второго списка — это границы, которые отдают
оставшееся обратно `git-repo-structure` и соседям.

## Одно добавление прогона CREATE, помеченное им самим

Абзац «Дом записи определяет вопрос, а не тема» (раздел 1) в задании не стоял;
прогон дописал его и пометил `proposed`, объяснив почему: ключевой триггер описания
— момент «в какой из трёх это ложится», и без ответа на него описание обещало бы
больше, чем несёт тело. Рабочий чат принял. Снимается одним резом, если владелец
сочтёт лишним, — тогда из описания уходит и второе предложение.
